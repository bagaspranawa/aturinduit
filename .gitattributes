# Normalise line endings so the repository stays LF regardless of the
# machine a clone happens on. Keeps diffs honest across Windows and others.
* text=auto eol=lf

# build.ps1 is run by Windows PowerShell, which is happiest with CRLF.
*.ps1 text eol=crlf

# Never let Git touch binary assets.
*.png binary
